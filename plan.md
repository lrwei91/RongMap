## RongMap 更新版计划：AI 内容提取录入 + 自动分类补全 + 检索 + 规则引擎

### Summary
短期主线从“AI 单条录入”升级为“多输入源地点提取与入库”：

- 支持在 AI 对话里提交地图定位信息、普通文本、抖音视频、抖音 URL
- 系统先从内容里提取地点线索，再搜索 POI、补全坐标和分类，最后按规则决定“自动保存”还是“向你确认”
- 短期先不做待复核中心和审计系统，重点放在接入、自动分类补全、检索、规则引擎
- 前端继续作为人工确认和检索操作台，后端补齐统一 intake 流水线

### Key Changes
#### 1. 统一内容入口
新增统一地点 intake 能力，支持四类输入：

- 地图定位信息：直接接收经纬度、地名、地址，优先做逆地理解析和 POI 补全
- 文本内容：从自然语言里提取地点名、商户名、区域名、地址片段
- 抖音 URL：抓取标题、文案、作者附文、可取得的字幕/描述，再抽取地点线索
- 抖音视频文件或转发内容：优先取附带文本，其次做语音转写/OCR，再抽取地点线索

统一处理流程：

1. 输入标准化
2. 提取候选地点线索
3. 调高德搜索/匹配 POI
4. 自动补全坐标、地址、城市、分类
5. 执行规则引擎
6. 返回 `saved` / `needs_confirmation` / `duplicate` / `not_found`

短期不做异步任务中心，先同步返回结果；只有内容提取耗时明显时，才预留任务化接口。

#### 2. 自动分类与自动补全
在现有 `category` 基础上增加自动推断，不要求 AI 或用户每次都显式传分类。

自动分类来源：

- 高德 POI 类型映射到现有分类枚举
- 名称关键词补充判断，例如“火锅/咖啡/茶室”归餐饮，“公园/景区/古镇”归景点
- 地址/商圈词兜底修正

自动补全内容：

- 缺坐标时补经纬度
- 缺地址时补标准化地址
- 缺名称时用 POI 主名称
- 缺分类时自动推断
- 对地图定位信息做逆地理补全，生成更稳定的 POI/地址描述

如果补全后置信度仍不足，则进入确认分支，而不是直接入库。

#### 3. 规则引擎
把“是否自动保存”从硬编码改成规则配置，短期先支持固定规则集。

首批规则：

- 餐饮：高置信默认优先自动保存
- 景点：低置信必须人工确认
- 同地址或 50 米内视为重复
- 名称 + 地址规范化完全一致直接判重
- 无法补全坐标的内容不自动保存
- 同一内容提取出多个候选地点时必须确认

建议规则结构：

- 输入：分类、置信度、候选数、是否有坐标、是否命中重复
- 输出：`auto_save`、`needs_confirmation`、`duplicate`、`reject`

短期先做代码内配置；后续如果规则变多，再升级成可编辑配置。

#### 4. 检索能力
前端从“列表浏览”升级为“可查找地点库”。

新增检索能力：

- 名称关键词搜索
- 地址关键词搜索
- 分类筛选和关键词组合
- 按“是否有坐标”筛选
- 按来源筛选：手动添加 / AI 文本 / AI 定位 / 抖音内容
- 列表实时筛选，地图同步展示结果

检索先放前端本地过滤；当数据量明显上升后，再补后端查询参数。

#### 5. 确认交互
短期保留“确认”能力，但不做完整待复核系统。

确认场景：

- 抖音内容提取出多个可能地点
- 景点类低置信
- 内容只提到商圈/片区，没有唯一 POI
- 命中疑似重复但无法直接合并

确认结果：

- 选定候选后再保存
- 允许用户放弃本次添加
- 返回明确提示：为什么需要确认

### Public Interfaces / Behavior
建议保留现有：

- `POST /api/openclaw/locations/intake`
- `POST /api/openclaw/locations/confirm`
- `GET/POST/PUT/DELETE /api/locations`

建议把 `intake` 扩成统一输入协议，支持：

- `inputType: "map_location" | "text" | "douyin_url" | "video"`
- `content`: 原始文本或 URL
- `locationPayload`: 地图定位信息
- `metadata`: 可选附加上下文，例如城市、来源平台、用户备注

建议补充地点字段：

- `sourceType`
- `sourcePlatform`
- `sourceContent`
- `confidence`
- `matchType`
- `poiType`
- `normalizedAddress`

响应保持简单：

- `saved`
- `needs_confirmation`
- `duplicate`
- `not_found`

### Test Plan
- 地图定位信息输入：
  - 能补全地址、名称、坐标
  - 单一高置信 POI 自动保存
- 文本输入：
  - 明确商户名时自动命中并保存
  - 模糊描述时返回候选确认
- 抖音 URL / 视频：
  - 能从标题/文案/字幕/OCR提取地点线索
  - 多候选时不误存，进入确认
  - 无地点信息时返回 `not_found`
- 自动分类：
  - 餐饮、景点、购物、交通等常见 POI 能稳定映射
  - 缺分类输入可自动补全
- 规则引擎：
  - 餐饮高置信自动保存
  - 景点低置信强制确认
  - 同地址或 50 米内重复能拦截
- 检索：
  - 关键词、分类、来源、是否有坐标可组合过滤
  - 列表和地图结果一致

### Assumptions
- 短期不做待复核面板、审计日志、任务中心
- 抖音内容提取优先使用“文本/URL可直接拿到的信息”；如果平台限制抓取，再降级到用户转发文本、字幕、OCR
- 城市仍默认优先福州，但保留从内容中识别城市的能力
- 规则引擎短期由后端配置文件或常量维护，不做 UI 配置页
- 前端检索短期先基于已加载数据本地过滤，不先做复杂后端搜索

如果你要，我下一步可以把这个计划再压成一版“2 周内可落地的实施顺序”，直接变成开发清单。